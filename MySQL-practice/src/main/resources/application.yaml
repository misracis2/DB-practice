spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: tmdduf
    password: tmdduf1!!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false

logging:
  level:
    org:
      springframework:
        jpa: DEBUG
        transaction: DEBUG