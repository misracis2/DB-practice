spring:
  datasource:
    url: jdbc:mysql://localhost:3307/test
    username: root
    password: tmdduf1!!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false

logging:
  level:
    org:
      springframework:
        jpa: DEBUG
        transaction: DEBUG